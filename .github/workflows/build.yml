name : sanity testing
on :
  push :
    branches:
       - master
jobs:
   cypress-run:
      runs-on: windows-latest
      steps : 
       - name: Checkout
         uses: actions/checkout@master
 
       - name : Cypress run
         uses : cypress-io/github-action@master
         with : 
          command : "npm run one:test"
